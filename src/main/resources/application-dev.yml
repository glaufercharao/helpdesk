spring:
  datasource:
    url: jdbc:mysql://localhost:3306/helpdesk
    username: root
    password: sasa
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        format_sql: true

